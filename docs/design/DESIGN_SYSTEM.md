# 디자인 시스템

> **작성일**: 2026-09-23
> **버전**: v1.0.0
> **정본 위치**: `src/styles/global.css`
> 색·간격·반경의 실제 값은 `global.css` 의 `:root` 가 정본입니다. 이 문서는 각 값의
> 역할과 근거를 설명합니다.

---

## 1. 두 레지스터

이 사이트는 성격이 다른 두 표면을 한 저장소에서 운영합니다.

| 레지스터 | 대상 페이지 | 배경 | 성격 |
| --- | --- | --- | --- |
| `nani` (라이트) | `/`, `/company/` | `#faf9f7` 계열 | 사람·회사·서사. 읽기 편한 에디토리얼 |
| `bidbox` (다크) | `/bidbox/` 이하 5종 | `#000000` 계열 | 데이터·기술·제품. 분석 도구의 인상 |

규칙:

- 한 페이지 안에서 두 레지스터를 섞지 않습니다.
- 다크 레지스터는 `BaseLayout` 의 `variant="bidbox"` 로 켜지며, `body` 에 `.reg-dark` 가
  붙습니다. 하위 페이지에서 배경을 다시 칠하지 않습니다.
- 라이트/다크 어느 쪽이든 **색은 토큰으로만** 지정합니다. 하드코딩한 16진수는 버그입니다.

---

## 2. 색 토큰

### 2.1 라이트 (nani)

| 토큰 | 값 | 역할 |
| --- | --- | --- |
| `--nani-bg` | `#faf9f7` | 페이지 바탕. 순백이 아닌 미세하게 따뜻한 회색 |
| `--nani-surface` | `#ffffff` | 카드 표면 |
| `--nani-ink` | `#0a1f33` | 본문 텍스트. 네이비를 머금은 검정 |
| `--nani-navy` | `#003066` | 강조 텍스트, 라벨, 아이콘 |
| `--nani-navy-deep` | `#001a38` | 그라디언트 하단 |
| `--nani-muted` | `#5a6b7b` | 보조 텍스트 |
| `--nani-border` | `#e6e9ef` | 경계선 |
| `--nani-tint` | `#eef4fb` | 선택 상태 배경, 태그 배경 |

### 2.2 다크 (bidbox)

| 토큰 | 값 | 역할 |
| --- | --- | --- |
| `--bb-bg` | `#000000` | 페이지 바탕 |
| `--bb-surface` | `#15181c` | 카드 표면. 순검정 위에서 경계를 만듦 |
| `--bb-surface-2` | `#0d1013` | 입력 필드 표면 |
| `--bb-navy` | `#042b4e` | 히어로 그라디언트 시작 |
| `--bb-navy-2` | `#094579` | 히어로 그라디언트 중간 |
| `--bb-accent` | `#b8ddf0` | 강조. 예측 금액, 링크, 태그 |
| `--bb-text` | `#f2f7fa` | 본문 텍스트 |
| `--bb-muted` | `#8fa3b3` | 보조 텍스트 |
| `--bb-border` | `rgba(184, 221, 240, 0.14)` | 경계선 |
| `--bb-border-strong` | `rgba(184, 221, 240, 0.3)` | 강조 카드 경계, 호버 |

### 2.3 공통 동작 색

| 토큰 | 값 | 역할 |
| --- | --- | --- |
| `--primary` | `#0073e6` | 주요 버튼. 제품 본체와 공유하는 브랜드 행동색 |
| `--primary-hover` | `#005bb3` | 버튼 호버 |
| `--primary-press` | `#004a94` | 버튼 눌림 |
| `--accent-ink` | `#042b4e` | 밝은 악센트 위에 얹는 텍스트 |

색 값의 출처는 원본 구성안 실측입니다. 근거는
[`DECK_TO_SITE_MAP.md`](DECK_TO_SITE_MAP.md) 의 색 추출 절을 참조하십시오.

---

## 3. 간격과 형태

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `--u1` | `4px` | 아이콘 내부, 인라인 간격 |
| `--u4` | `16px` | 문단 간격, 카드 내부 패딩 기본 |
| `--u9` | `36px` | 섹션 전환, 큰 블록 간격 |
| `--radius` | `14px` | 입력 필드, 작은 표면 |
| `--radius-lg` | `22px` | 카드 |
| `--shadow-soft` | 2단 그림자 | 라이트 카드 |
| `--shadow-deep` | 1단 큰 그림자 | 다크 표면 |
| `--ease-out` | `cubic-bezier(0.22, 1, 0.36, 1)` | 모든 전환 |

간격은 4px 배수만 씁니다. 목록에 없는 임의 값을 만들지 않습니다.

---

## 4. 타이포그래피

| 항목 | 값 |
| --- | --- |
| 본문 폰트 | `"Pretendard Variable", Pretendard, -apple-system, ...` |
| 로딩 | jsDelivr Pretendard 가변 서브셋 |
| 본문 크기 | `1rem` |
| 본문 행간 | `1.5` |
| 다크 레지스터 본문 행간 | `1.72` (밝은 글자는 광학적으로 얇게 보이므로 보정) |
| 줄바꿈 | `word-break: keep-all` (한국어 어절 보존) |

타입 클래스:

| 클래스 | 역할 |
| --- | --- |
| `.display` | 최상위 제목. 900 웨이트, 자간 `-0.03em`, 행간 1.08 |
| `.eyebrow` | 섹션 라벨. 대문자, 자간 `0.18em`, 11px |
| `.lede` | 도입 문단. 최대 `68ch` |
| `.measure` | 본문 폭 제한 |
| `.num` | 숫자 정렬 (`tabular-nums`) |
| `.strike` | 취소선 금액 |

한국어 사이트이므로 제목에 `text-wrap: balance` 를 쓰지 않고, 문장 중간 `<br />` 로
호흡을 잡습니다.

---

## 5. 컴포넌트

| 클래스 | 역할 | 주요 상태 |
| --- | --- | --- |
| `.btn` | 기본 버튼. 최소 높이 48px, 완전 둥근 모서리 | hover, active, disabled |
| `.btn-primary` | 주요 행동 | 배경 변경 |
| `.btn-ghost` | 보조 행동 | 경계 강조 |
| `.btn-accent` | 다크 표면 위 주요 행동 | 밝은 배경 |
| `.btn-block` | 폭 100% | - |
| `.card` | 표면 컨테이너 | - |
| `.card-lift` | 카드 호버 상승 | translateY(-4px), 경계 강조 |
| `.tag` | 라벨 칩 | - |
| `.field` | 라벨 + 입력 + 오류 묶음 | `is-invalid` |
| `.nav-link` | 내비 링크. 최소 높이 44px | hover, `aria-current="page"` |
| `.nav-link--block` | 모바일 드로어용 전폭 변형 | - |
| `.site-nav` | 상단 고정 내비 + 블러 배경 | - |
| `.wrap` | 최대 1180px 컨테이너 | - |
| `.section` / `.section-tight` | 섹션 상하 여백 | - |
| `.rule` | 구분선 | - |
| `.skip-link` | 본문 건너뛰기 | focus 시 노출 |
| `.rise` | 진입 모션 | `prefers-reduced-motion` 시 비활성 |

컴포넌트 클래스가 Tailwind 유틸리티와 충돌할 때는 **유틸리티에 important 접미사**를
붙여 우선합니다. 예: `<li class="card p-0!">`.
Tailwind 4 에서 접두사 형태(`!p-0`)는 무효이므로 쓰지 않습니다.

---

## 6. 포커스와 모션

**포커스**는 모든 대화형 요소에 대해 점선 2px, 오프셋 3px, `--primary` 색으로
표시합니다. 다크 레지스터에서는 `--bb-accent` 로 바뀝니다. `outline: none` 만 쓰는
것을 금지합니다.

**모션**은 다음 원칙만 따릅니다.

1. `transform` 과 `opacity` 만 애니메이션합니다. 레이아웃 속성을 움직이지 않습니다.
2. 진입은 `.rise` 클래스로 0.7s ease-out, 필요 시 0.08s 간격으로 순차 적용합니다.
3. `prefers-reduced-motion: reduce` 에서는 모든 애니메이션과 전환이 0.01ms 로 줄고
   부드러운 스크롤이 해제됩니다.
4. 상태 변화를 설명하지 못하는 전환은 넣지 않습니다.

---

## 7. 하지 않는 것

- 색상 16진수를 페이지에 직접 쓰기
- Tailwind CDN 사용 (프로덕션에서 경고 발생, 빌드 시점 최적화 없음)
- React 등 UI 프레임워크 도입
- Alpine 을 쓰는 지점 확대 (`AGENTS.md` 2장의 3개소 외)
- `x-cloak` 과 `x-show` 와 `x-transition` 을 같은 요소에 함께 쓰기
  ([`../ops/DO_NOT_REPEAT.md`](../ops/DO_NOT_REPEAT.md) 참조)
